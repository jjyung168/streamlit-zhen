- name: Ping Streamlit app
  run: |
    URL="https://app-zhen-gbvangddctfuxkqfqrfbrd.streamlit.app/"
    code=$(curl -sS -o /dev/null -m 30 -w "%{http_code}" -A "Mozilla/5.0" "$URL")
    echo "HTTP $code"
    # 2xx/3xx 都算成功；只有 4xx/5xx 才失败
    if [ "$code" -ge 400 ]; then exit 1; fi
