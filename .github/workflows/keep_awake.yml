name: keep-awake

on:
  schedule:
    - cron: "0 */6 * * *"   # 每 6 小时访问一次
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -L -m 30 -sS "https://app-zhen-gbvangddctfuxkqfqrfbrd.streamlit.app/" > /dev/null
